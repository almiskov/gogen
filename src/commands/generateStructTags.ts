import * as vscode from "vscode";

export class GenerateStructTagsCommand implements vscode.Command {
    title: string = '';
    command: string = '';
    tooltip?: string | undefined;
    arguments?: any[] | undefined;

    // TODO: command to add struct tags
}