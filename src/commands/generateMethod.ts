import * as vscode from "vscode";

export class GenerateMethodCommand implements vscode.Command {
    title: string = '';
    command: string = '';
    tooltip?: string | undefined;
    arguments?: any[] | undefined;

    // TODO: command to generate method
}